run = "cd /workspace/1_foundations && exec python -m uvicorn app:app --host 0.0.0.0 --port 8000"
modules = ["python-3.11"]

[nix]
packages = ["lsof", "python311", "python311Packages.pip", "python311Packages.uvicorn"]

[[ports]]
localPort = 8000
externalPort = 8000

[agent]
expertMode = true

[deployment]
deploymentTarget = "cloudrun"
